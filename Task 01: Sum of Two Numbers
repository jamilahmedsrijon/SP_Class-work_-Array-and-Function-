#include<stdio.h>

int add(int num1, int num2) {
    return num1 + num2;
}

int main() 
{
    int a = 10, b = 5;
    int result = add(a, b);
    printf("The sum is %d\n",result);
    
    return 0;
}
